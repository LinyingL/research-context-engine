"""Deterministic ingestion layer (DESIGN.md section 0: no model calls
here -- parser/regex/subprocess only). git history lands first
(rce.ingest.git); LaTeX/.bib/W&B ingesters follow in later tasks per
DESIGN.md section 7.
"""
